# 项目代理规则

## 最小上下文

- 先阅读目标文件及其直接关联的样式、配置和调用处；不要为小改动扫描整个仓库。
- 仅在涉及跨模块、分包、路由或架构取舍时阅读 `.agents/repowiki.md`。
- 保留用户已有的未提交改动；只修改本次任务涉及的文件。

## 规则与技能路由

- 修改 `.ts`/`.tsx` 时，遵守 `.agents/rules/react-typescript.mdc`。
- 涉及页面、路由、Service 或 API 调用时，再读取 `.agents/rules/web-layer.mdc`；仅 CRUD 管理页读取 `web-page-layer.mdc`。
- 新建移动端页面时优先使用 `harness-create-mobile-page`；新增 CRUD 管理页时优先使用 `harness-create-web-page`。
- 用户要求前端审查或提交前审查时，优先使用 `harness-review-web`。
- Bug 或异常行为先定位根因；优先使用 `systematic-debugging`。只有需要项目修复记录时，再使用项目 BugFix 工作流。
- 新增可测试的业务逻辑时优先采用 TDD；纯文案、样式和配置改动不强制新增测试。

## Superpowers 使用边界

- 只调用与当前任务直接匹配的 Superpowers 技能；不要因简单、已明确的改动创建设计文档、工作树或子代理。
- 复杂且存在多种实现路径的功能，在编码前先说明推荐方案和影响范围。
- 宣称完成前必须基于本次任务的实际检查结果；优先运行最小相关检查。

## 交付与验证

- 修改代码后审查本次差异：范围、类型、逻辑、样式/布局与回归风险。
- 不自动运行构建；仅在用户明确要求时运行。优先使用定向类型、格式、单测或资源校验，并如实说明未执行的验证。
- 最终回复只报告变更、验证与残留风险；无需重复过程或未使用的技能。
